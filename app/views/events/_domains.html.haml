- @event.domains.each do |d|
  .domain.grey_bar
    = d.domain
    = link_to('X', d, :remote=>true, :method => :delete) if d.domain
- flash.each do |name, msg|
  = content_tag :div, msg, :id => "flash_#{name}"
  - flash.clear
