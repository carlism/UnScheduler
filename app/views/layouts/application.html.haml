!!!
%html
  %head
    %title 
      Unscheduler : 
      = yield(:title) || "The BarCamp/Unconference Scheduler"
    = stylesheet_link_tag 'screen.css', :media => 'screen, projection'
    = stylesheet_link_tag 'print.css', :media => 'print'
    /[if lt IE 8]
      = stylesheet_link_tag 'ie.css', :media => 'screen, projection'
    = stylesheet_link_tag "jquery-ui-1.8.9.custom"
    = javascript_include_tag :defaults
    = csrf_meta_tag
  %body.two-col
    #header
      #user_nav.float-right
        - if user_signed_in?
          = current_user.email
          = link_to "Sign out", destroy_user_session_path
        - else
          = link_to "Sign in/Sign up", new_user_session_path, :remote => true
      %h1 UnScheduler
      The BarCamp/unconference scheduler
      .notice= notice
      .alert= alert
    #content
      = yield
    #sidebar
      %p
        %a{:href=>"mailto:support@unscheduler.com"} Contact Us
      = yield(:sidebar)
      %p 
        This site is powered by open source software. Learn more 
        %a{:href=>"http://github.com/carlism/unscheduler"} where we maintain the code
    #dialog_box
    #fade.black_overlay
